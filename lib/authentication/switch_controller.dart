import 'package:flutter/material.dart';

class SwitchController{
  // shared bw both the screen for switch bw the screen
  static final ValueNotifier<int> currentScreenIndex = ValueNotifier<int>(0);
}
